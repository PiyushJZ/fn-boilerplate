{
  "name": "fn-boilerplate",
  "description": "Fullstack boilerplate using Fastify and Nuxt",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^9.0.0"
  },
  "scripts": {
    "dev": "lerna run dev --parallel"
  },
  "engines": {
    "node": ">=24.11.0"
  }
}